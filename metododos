public Static void main (int[] Arrayuno, int[] Arrraydos )
{
bool result;
  for(int i=0; i<Arrayuno.length; i++)
   {
     for(int c=0; c<Arraydos.length; c++){
      if(Arrayuno[i]==Math.sqrt(Arraydos[c]))
      result=true
      }
      else{
       result=false
      }
   }
}
