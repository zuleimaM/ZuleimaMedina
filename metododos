public Static void CompararRaiz (int[5] Arrayuno, int[5] Arrraydos )
{
bool result;
if(Arrayuno==null)││(arraydos==null){
 result=false
}
else{
  for(int i=0; i<Arrayuno.length; i++)
   {
     for(int c=0; c<Arraydos.length; c++){
      if(Arrayuno[i]==Math.sqrt(Arraydos[c]))
      result=true
      }
      else{
       result=false
      }
   }
   }
     System.out.println(result);
}
